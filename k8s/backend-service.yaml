apiVersion: v1
kind: Service

metadata:
  name: backend-service
  labels:
    app: backend

spec:
  type: NodePort

  selector:
    app: backend

  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 32762